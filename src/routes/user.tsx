export default function UserPage() {
  return (
    // case value > treshold? render colored graphic depending on level.
    // change color of icons in sidebar? red bad yellow ok green great? maybe change levels, experiment with custom continuious color ran ge
    // show average by day

    // do i constantly pull all data? do i rerfresh every n minutes? do i have it be on clicking a new element?
    // I really want to emphasize the live nature of the icon colors.
    // current strategy proposal: 
    <>user page</>
  );
}
